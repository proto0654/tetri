---
paths:
  - 'app/Models/*.php'
---

# Models

## Category image and columns
Category must include image (homepage menu-preview cards) and columns (2 or 3 for /menu grid). Mockup overrides any entity summary that omits these fields.
