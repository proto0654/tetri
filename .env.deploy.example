# Local env for demo:pull / demo:push (copy to .env.deploy — never commit .env.deploy)
# Fill real host/user/path/key locally. Never commit account IDs or panel passwords.

DEPLOY_HOST=
DEPLOY_USER=
DEPLOY_PATH=
DEPLOY_SSH_KEY=
DEPLOY_PHP=/opt/php/8.4/bin/php
